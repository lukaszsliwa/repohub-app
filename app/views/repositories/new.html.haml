= content_for :nav do
  = render partial: 'shared/nav'

= form_for @repository, as: :repository, url: repositories_path, html: {multipart: true} do |f|
  = render partial: 'repositories/form', locals: {f: f}